# 시나리오 03: 주문 접수 → 승인(PRODUCING) → 자동완료 → CONFIRMED
# 사전 조건: -ResetData 권장
# 예상 결과:
#   - 시료 S-P003: 재고 10, 주문량 100 → 부족(shortage=90)
#   - avgProductionTime=0.001 (sec/ea) → totalTime ≈ 0.1초 (즉시 완료 가능)
#   - 블록 1 실행 후 2초 대기 → 블록 2에서 checkAndAutoComplete() 호출 시 CONFIRMED 전환
#   - 모니터링: CONFIRMED 1건, 재고 = 실생산량(112) - 주문량(100) = 12 확인

# ════════════════════════════════════════════════
# 블록 1: 시료 등록 → 주문 → 승인(PRODUCING) → 생산라인 확인
# ════════════════════════════════════════════════

# ── 시료 등록 (재고 10으로 의도적 부족 유도) ────────────
1
1
S-P003
자동완료테스트시료
0.001
0.90
10
0

# (계속하려면 Enter)

# ── 주문 접수 (100개 주문, 재고 10 < 100 → PRODUCING 예상) ─
2
S-P003
현대반도체
100

# (계속하려면 Enter)

# ── 주문 승인 → PRODUCING 전환 ──────────────────────────
3
ORD-{TODAY}-001
1

# (계속하려면 Enter)

# ── 생산라인 현황 확인 (PRODUCING 중임을 보여줌) ──────────
5

# (계속하려면 Enter)

# ── 앱 종료 (블록 1 끝) ───────────────────────────────
0

!sleep 2

# ════════════════════════════════════════════════
# 블록 2: 재진입 → 자동완료 트리거 → 모니터링 확인
# (메인 루프 진입 시 checkAndAutoComplete() 자동 호출)
# ════════════════════════════════════════════════

# ── 모니터링: CONFIRMED 전환 + 재고 증가 확인 ──────────
4

# (계속하려면 Enter)

# ── 종료 ───────────────────────────────────────────────
0
